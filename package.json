{
  "name": "extract",
  "version": "3.1.0",
  "private": true,
  "description": "Allows to extract archive from the web interface.",
  "license": "agpl",
  "author": "Antoon Prins <antoon.prins@surf.nl>",
  "contributors": [],
  "type": "module",
  "browser": {
    "fs": false
  },
  "scripts": {
    "build": "vite build --mode production",
    "dev": "vite build --mode development",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "stylelint": "stylelint src/**/*.scss src/**/*.css --allow-empty-input",
    "stylelint:fix": "src/**/*.scss src/**/*.css --fix",
    "typescript:check": "tsc --noEmit",
    "typescript:generate": "npx openapi-typescript ./openapi.json -t -o src/types/openapi/openapi.ts",
    "watch": "vite build --mode development --watch"
  },
  "browserslist": [
    "extends @nextcloud/browserslist-config",
    "not chrome 109",
    "not edge 109",
    "not ios_saf 11"
  ],
  "dependencies": {
    "@mdi/svg": "^7.4.47",
    "@nextcloud/axios": "^2.6.0",
    "@nextcloud/files": "^3.12.2",
    "@nextcloud/l10n": "^3.4.1",
    "@nextcloud/logger": "^3.0.3",
    "@nextcloud/router": "^3.1.0"
  },
  "devDependencies": {
    "@nextcloud/browserslist-config": "^3.1.2",
    "@nextcloud/eslint-config": "^9.0.1",
    "@nextcloud/stylelint-config": "^3.2.2",
    "@nextcloud/vite-config": "^3.0.0-beta.0",
    "esbuild-loader": "^4.3.0",
    "openapi-typescript": "^7.13.0",
    "ts-loader": "^9.5.4",
    "vite": "^8.1.4"
  },
  "overrides": {
    "js-yaml": "4.3.2"
  },
  "engines": {
    "node": ">=24",
    "npm": "^10.5.0 || ^11.0.0"
  }
}
